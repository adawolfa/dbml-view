/// <reference types="vite/client" />

/**
 * Replaced at build time with the git-derived app version (see
 * scripts/version.mjs and vite.config.ts `define`).
 */
declare const __APP_VERSION__: string;
