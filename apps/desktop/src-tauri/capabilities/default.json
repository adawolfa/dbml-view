{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability — read-only viewer needs only core IPC.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
